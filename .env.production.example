# PeerJS Server Configuration for Production
# Copy this file to .env.production if you need custom production settings

# Option 1: Public PeerJS Server (default, no configuration needed)
# The app will use 0.peerjs.com if no variables are set

# Option 2: Your own PeerJS server
# Uncomment and configure if hosting your own server:
# VITE_PEER_HOST=your-peerjs-server.com
# VITE_PEER_PORT=443
# VITE_PEER_PATH=/myapp
# VITE_PEER_SECURE=true
